import { Typography } from '../../elements/Typography';
import { useSettingsScreenStyle } from './LoginScreen.styles';

export const LoginScreen = () => {
  const styles = useSettingsScreenStyle();

  return <Typography variant="h1">login</Typography>;
};
