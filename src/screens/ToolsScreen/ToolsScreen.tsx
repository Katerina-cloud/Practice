import { Typography } from '../../elements/Typography';
import { useToolsScreenStyle } from './ToolsScreen.styles';

export const ToolsScreen = () => {
  const styles = useToolsScreenStyle();

  return <Typography variant="h1">Tools</Typography>;
};
